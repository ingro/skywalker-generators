import EditViews from 'application/views/edit';

const EditLayout = EditViews.Layout.extend({});

const Title = EditViews.Title.extend({
    template: '<$ name $>/edit/title'
});

const Edit = EditViews.EditPlain.extend({
    template: '<$ name $>/edit/edit'
});

export default {
    EditLayout,
    Title,
    Edit
};